import { AddFlightRequestModel } from './add-flight-request-model';

describe('AddFlightRequestModel', () => {
  it('should create an instance', () => {
    expect(new AddFlightRequestModel()).toBeTruthy();
  });
});
