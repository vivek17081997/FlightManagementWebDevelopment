import { SearchFlightRequestModel } from './search-flight-request-model';

describe('SearchFlightRequestModel', () => {
  it('should create an instance', () => {
    expect(new SearchFlightRequestModel()).toBeTruthy();
  });
});
